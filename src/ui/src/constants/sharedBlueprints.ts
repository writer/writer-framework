/**
 * Default organization ID to use in local development when writerOrgId is not available.
 * This matches the development mode behavior in be.agent-storage.
 */
export const DEFAULT_ORG_ID = 72564;

